color jellybeans+

" Don't show scrollbars
set guioptions-=r
set guioptions-=L
set guioptions-=T

" MacVim
if has('gui_macvim')
  set fuoptions=maxvert,maxhorz " Full-screen mode uses the full screen
endif
